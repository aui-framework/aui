cmake_minimum_required(VERSION 3.10)

aui_executable(aui.example.cells)
if (MSVC AND AUI_BUILD_FOR STREQUAL "winxp")
    include(../../../WinXPuseLTL5.cmake)
    aui_link(aui.example.cells PRIVATE aui::views YY_Thunks)
    set_target_properties(aui.example.cells PROPERTIES LINK_FLAGS "/SUBSYSTEM:WINDOWS,5.01 /ENTRY:WinMainCRTStartup")
else()
    aui_link(aui.example.cells PRIVATE aui::views)
endif()
aui_enable_tests(aui.example.cells)
