#pragma once


/**
 * @brief Supported channels formats  (mono, stereo etc.)
 */
enum class AChannelFormat {
    /**
     * @brief Mono sound
     */
    MONO = 1,
    /**
     * @brief Stereo sound
     */
    STEREO = 2
};